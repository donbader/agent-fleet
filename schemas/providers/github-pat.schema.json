{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/donbader/agent-fleet/schemas/providers/github-pat.schema.json",
  "title": "GitHub PAT egress rule options",
  "description": "Options for the github-pat egress rule provider",
  "type": "object",
  "required": ["token"],
  "additionalProperties": false,
  "properties": {
    "token": {
      "type": "string",
      "description": "GitHub PAT token. Use ${VAR} syntax to reference .env variables.",
      "pattern": "^\\$\\{.+\\}$",
      "examples": ["${GITHUB_PAT_TOKEN}"]
    }
  }
}
