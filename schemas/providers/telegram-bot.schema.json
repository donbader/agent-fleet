{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/donbader/agent-fleet/schemas/providers/telegram-bot.schema.json",
  "title": "Telegram bot egress rule options",
  "description": "Options for the telegram-bot egress rule provider (URL path rewrite)",
  "type": "object",
  "required": ["token"],
  "additionalProperties": false,
  "properties": {
    "token": {
      "type": "string",
      "description": "Telegram bot token. Use ${VAR} syntax to reference .env variables.",
      "pattern": "^\\$\\{.+\\}$",
      "examples": ["${TELEGRAM_BOT_TOKEN}"]
    }
  }
}
