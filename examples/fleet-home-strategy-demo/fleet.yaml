# yaml-language-server: $schema=../../schemas/fleet.schema.json

fleet:
  name: home-strategy-demo

agents:
  - ephemeral
  - named-volume
  - custom-base

egress-presets:
  allow-all:
    - host: ["*"]
