# Generated from fleet config — fill in your values.
# Variables referenced via ${VAR} in fleet.yaml / agent.yaml.

# Channel credentials (each agent has its own bot)
TELEGRAM_BOT_TOKEN=

# For multi-agent setups:
# TELEGRAM_BOT_TOKEN_CODER=
# TELEGRAM_BOT_TOKEN_REVIEWER=

# GitHub PAT (injected by proxy via header)
GITHUB_PAT_TOKEN=

# MCP OAuth uses dynamic client registration — no client credentials needed
